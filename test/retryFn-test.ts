/* eslint-disable no-undef */
import { retryFn } from "../src/index";
import { describe } from "mocha";
import { expect } from "chai";

describe("retryFn test", () => {
    it("", () => {
        expect();
    });
});
